<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MeshDrop.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:MeshDrop.Models"
    xmlns:vm="using:MeshDrop.ViewModels">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 顶部卡片：本机信息 -->
        <Grid Grid.Row="0" Margin="16" Padding="16" CornerRadius="12"
              Background="{ThemeResource LayerFillColorDefaultBrush}"
              BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
              BorderThickness="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <FontIcon Glyph="&#xE701;" FontSize="24" VerticalAlignment="Center" Margin="0,0,12,0" />
            <StackPanel Grid.Column="1">
                <TextBlock Text="{x:Bind ViewModel.DisplayName, Mode=OneWay}"
                           FontSize="16" FontWeight="SemiBold" />
                <TextBlock Text="{x:Bind ViewModel.FingerprintShort, Mode=OneWay}"
                           FontFamily="Cascadia Mono"
                           FontSize="12" Opacity="0.7" />
            </StackPanel>
        </Grid>

        <!-- 设备列表 -->
        <ListView Grid.Row="1"
                  ItemsSource="{x:Bind ViewModel.Devices, Mode=OneWay}"
                  Margin="8,0,8,8"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:DeviceVM">
                    <Grid Padding="12" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <FontIcon Glyph="{x:Bind IconGlyph}" FontSize="22"
                                  VerticalAlignment="Center" />
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" />
                            <TextBlock Text="{x:Bind Subtitle}" FontSize="12" Opacity="0.7" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- 空状态 -->
        <StackPanel Grid.Row="1"
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    Visibility="{x:Bind ViewModel.EmptyStateVisibility, Mode=OneWay}">
            <FontIcon Glyph="&#xE721;" FontSize="48" Opacity="0.5" />
            <TextBlock Text="正在搜索附近设备…" FontSize="16" Margin="0,12,0,0"
                       HorizontalAlignment="Center" />
            <TextBlock Text="确保对方设备在同一 Wi-Fi 下并已启动 MeshDrop"
                       FontSize="12" Opacity="0.6" Margin="0,4,0,0"
                       HorizontalAlignment="Center" />
        </StackPanel>
    </Grid>
</Window>
